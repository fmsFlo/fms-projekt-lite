<!DOCTYPE html>
<html lang="de">
  <head>
    <meta charset="utf-8" />
    <style>
      * { box-sizing: border-box; margin: 0; padding: 0; }
      body {
        font-family: Arial, sans-serif;
        font-size: 11pt;
        line-height: 1.5;
        color: #000;
        padding: 25mm 25mm 20mm 25mm;
        max-width: 210mm;
      }
      .recipient-block { margin-bottom: 12mm; line-height: 1.4; }
      .sender-address { text-align: right; margin-bottom: 10mm; line-height: 1.4; }
      .date-line { text-align: right; margin-bottom: 12mm; }
      .subject { font-weight: bold; margin-bottom: 8mm; }
      p { margin-bottom: 5mm; text-align: justify; }
      p.no-justify { text-align: left; }
      ul {
        margin: 0 0 5mm 5mm;
        padding-left: 5mm;
      }
      .signature-area { margin-top: 15mm; }
    </style>
  </head>
  <body>
    <div class="recipient-block">
      {{recipientCompany}}<br />
      {{#if recipientAddressLines}}
        {{#each recipientAddressLines}}
          {{this}}<br />
        {{/each}}
      {{/if}}
      {{#if recipientEmail}}
        {{recipientEmail}}
      {{/if}}
    </div>

    <div class="sender-address">
      {{client.firstName}} {{client.lastName}}<br />
      {{#if client.street}}{{client.street}} {{client.houseNumber}}<br />{{/if}}
      {{#if client.zip}}{{client.zip}} {{client.city}}{{/if}}<br />
      {{#if client.email}}E-Mail: {{client.email}}{{/if}}
    </div>

    <div class="date-line">
      {{#if client.city}}{{client.city}}, {{/if}}den {{date}}
    </div>

    <div class="subject">
      Anforderung von Unterlagen – Vertrag {{policyNumber}}
    </div>

    <p class="no-justify">
      Sehr geehrte Damen und Herren{{#if contactPerson}},<br />{{contactPerson}}{{/if}},
    </p>

    <p>
      für die weitere Bearbeitung benötige ich die nachstehenden Unterlagen zum oben genannten Vertrag. Ich bitte Sie, mir diese vollständig zur Verfügung zu stellen.
    </p>

    {{#if requestedDocumentList.length}}
      <ul>
        {{#each requestedDocumentList}}
          <li>{{this}}</li>
        {{/each}}
      </ul>
    {{else}}
      <p>
        Bitte übersenden Sie mir sämtliche vertragsrelevanten Unterlagen, inklusive Angebot, Versicherungsschein, Bedingungen und bisheriger Standmitteilungen.
      </p>
    {{/if}}

    {{#if customBody}}
      <p>{{customBody}}</p>
    {{/if}}

    <p>
      Bitte senden Sie mir die Unterlagen bis spätestens {{responseDeadline}} per E-Mail{{#if client.email}} ({{client.email}}){{/if}} oder Post zu.
    </p>
    <div class="signature-area">
      <p class="no-justify">Mit freundlichen Grüßen</p>
    </div>
     <p class="no-justify" style="margin-top: 12mm;">
      {{client.firstName}} {{client.lastName}}
       {{#if client.email}}<br /><span style="font-size: 9pt; color: #444;"></span>{{/if}}
    </p>
  </body>
</html>

